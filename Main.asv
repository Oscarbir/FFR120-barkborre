clc, clear all, close all;

N=16;
p=0.7;
densityOfFores
f=0.2;
[B, T] = xlsread('temperaturdata.xlsx');

forest=initForest(N,p);
%%
nrOfTrees=[];

while length(nrOfTrees)<500 
    tree=0;
    forest=growth(forest,p,N);
    forest=spreading(forest,f,N,tree,nrOfTrees); 

end
    